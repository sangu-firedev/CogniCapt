# CogniCapt
AI image captioning system. It uses DeepLearning to generate a caption for a given image by user. CNN model like ResNet is used to extract the features of
the image, the extracted features is used as input for RNN models to generate a sequence of words forming a caption for the image.

